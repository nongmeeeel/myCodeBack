spring:
  application:
    name: mycodeBack
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/mycodedb
    username: mycode
    password: qwe321!!

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true